<h1>Demo#index</h1>
<p>Hello usc.</p>
<br/>
<p>The fucking number is <%= @number %></p>
<table>
	<tr>
		<% @user.each do |usr| %>
    <% @doctor.each do |doc| %>
    <% if usr.user_id === doc.doctor_id %>
    <td><%= usr.first_name+' '+usr.last_name+'Designstion ='+doc.designation%></td>
    <%end%>
    <%end%>
	</tr>

<%end%>